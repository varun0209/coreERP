import { FocusOnEnterDirective } from './focus-on-enter.directive';

describe('FocusOnEnterDirective', () => {
  it('should create an instance', () => {
    const directive = new FocusOnEnterDirective();
    expect(directive).toBeTruthy();
  });
});
